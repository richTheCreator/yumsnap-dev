var db = require('../db');

var Session = module.exports

Session.create = function (userId) {
  // TODO: Uncomment after you create your sessions table schema
  // TODO: Generate uuid
  // var id = ...
  // return db('sessions').insert({ id: id, user_id: userId })
}

Session.findById = function (sessionId) {
  // TODO: Write after you create your sessions table schema
}
